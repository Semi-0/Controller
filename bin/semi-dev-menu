#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
JAR="$ROOT/target/semi-dev-menu.jar"
if [[ ! -f "$JAR" ]]; then
  echo "Missing $JAR — run from project root: clojure -T:build uber" >&2
  exit 1
fi
exec java -jar "$JAR" "$@"
